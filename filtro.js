const teste = data.video
console.log(teste.filter((elem) => elem.id == 1))

const filterItens = (qrcode) => { 
  const filteredItens2 = menu.filter(element => element.id)
  if (qrcode === element.id){ 
    return filterItens2
  }
}
